# Models

Mongoose data models for Medtachy live here (e.g. `User`, `Patient`,
`MedicalRecord`, `Appointment`). Each model is defined in its own file and
exported for use by the service layer.

This folder is intentionally empty for now and will be populated as
data-driven features are implemented.
